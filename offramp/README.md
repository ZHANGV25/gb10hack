# Offramp (working name)

Second version of the problem behind ExitPlan: **the harness that makes models fungible** — moving
an institution's judgment out of vendor weights into a database it owns, so frontier→local (or any
model swap) becomes a measured dial instead of a leap.

Start here: [`docs/00-thesis.md`](docs/00-thesis.md)

Planned: `docs/01-target.md` (who buys — next session) · `docs/02-pitch.md` (deck).
The hackathon desk in `apps/` is the first instance and evidence base; `deck/` is its pitch.
